package accessModifiersConcept2;
import accessModifiersConcept1.*;

//Here we try to access java class in different package where that class name is in default Access Modifier so we can't access that in other package until we declare in any other access modifier
public class addtionOperation1DOT1 {

	public static void main(String[] args) {
		
		//addtionOperation obj = new addtionOperation();
		//obj.addtion(100, 200);
//Error ,when declare that class we try to access in public we can able to access that

	}

}
